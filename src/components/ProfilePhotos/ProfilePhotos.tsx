import { observer } from "mobx-react-lite";

const ProfilePhotos = () => {
    return (
        <div>
            <h1>Profile Photos</h1>
        </div>
    );
}

export default observer(ProfilePhotos);